{include file=$oView->getTemplateFile('header','/shared') pageTitle="show"}

		<h1>show</h1>
		<p>This is the standard view for showController. You will now have to customise it along with the controller, model and view.</p>

{include file=$oView->getTemplateFile('footer','/shared')}