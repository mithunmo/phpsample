{include file=$oView->getTemplateFile('header','/shared') pageTitle="cart"}

		<h1>cart</h1>
		<p>This is the standard view for cartController. You will now have to customise it along with the controller, model and view.</p>

{include file=$oView->getTemplateFile('footer','/shared')}