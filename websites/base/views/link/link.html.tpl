{include file=$oView->getTemplateFile('header','/shared') pageTitle="link"}

		<h1>link</h1>
		<p>This is the standard view for linkController. You will now have to customise it along with the controller, model and view.</p>

{include file=$oView->getTemplateFile('footer','/shared')}