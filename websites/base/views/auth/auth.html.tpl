{include file=$oView->getTemplateFile('header','/shared') pageTitle="auth"}

		<h1>auth</h1>
		<p>This is the standard view for authController. You will now have to customise it along with the controller, model and view.</p>

{include file=$oView->getTemplateFile('footer','/shared')}